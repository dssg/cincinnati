source:
  #URL is optional. If present, script will download it every time and saved using
  #filename value. Otherwise, script will load file named as filename value
  url: https://open-data.org/file.csv
  filename: data.csv
  column: DATE
storage:
  schema: public
  table: cool_dataset
  column: date